import React from 'react'

type HeaderProps = {
    isDarkMode : boolean
}

const Header = ({isDarkMode}: HeaderProps) => {
    return (
        <>
            
        </>
    )
}

export default Header